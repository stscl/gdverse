# NTDs data

The data were obtained by preprocessing use `sf` and `tidyverse`.

## Usage

``` r
NTDs
```

## Format

`NTDs`: A tibble with 185 rows and 4 variable columns and 2 location
columns, modified from `geodetector` package.
